
{{!-- Renderiza la la pagina para editar las datos --}}
<section class="home-container">
    <div class="edit-container card-singup">
        <div class="card-header">
            <h3 class="text-center">Apdate a Task</h3>
        </div>

    <div class="cad-body">
        <form class="edit-form contact-form" action="/tasks/edit/{{tasks._id}}" method="post">
            <input type="text" name="title" placeholder="Title" value="{{tasks.title}}" class="">
            <textarea name="description" placeholder="Description" rows="3">
            {{tasks.description}}
            </textarea>
            <button class="btn">Update</button>
    </form>
    </div>
</div>
</section>